{
  "name": "pomodoro-timer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "test": "node --input-type=module -e \"import { webcrypto } from 'node:crypto'; if (typeof globalThis.crypto?.getRandomValues !== 'function') { Object.defineProperty(globalThis, 'crypto', { value: webcrypto, configurable: true }); } process.argv = ['node', 'vitest', 'run']; await import(new URL('./node_modules/vitest/vitest.mjs', import.meta.url));\"",
    "test:watch": "node --input-type=module -e \"import { webcrypto } from 'node:crypto'; if (typeof globalThis.crypto?.getRandomValues !== 'function') { Object.defineProperty(globalThis, 'crypto', { value: webcrypto, configurable: true }); } process.argv = ['node', 'vitest']; await import(new URL('./node_modules/vitest/vitest.mjs', import.meta.url));\""
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "motion": "^12.42.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "tailwindcss": "^4.3.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "jsdom": "^29.1.1",
    "typescript": "~5.8.3",
    "vite": "^7.0.4",
    "vitest": "^4.1.9"
  }
}
